.DS_Store
*.lscache
/obj
/bin
/wwwroot/App_Plugins/le-løgin

# Ignore build artefacts in nested projects (for example test projects)
**/bin/
**/obj/
/bin
bin

# Agent skills (development-only tooling, not part of the package)
/.agents/
/skills-lock.json

# Editor schemas copied into each project by Umbraco.Cms.Targets at build time. They are
# dev-time IntelliSense aids, not source — and the class library no longer references the
# meta-package that regenerates them, so tracking them would freeze stale copies.
appsettings-schema.json
appsettings-schema.*.json
umbraco-package-schema.json

# Azurite emulator state written by the Azure Blob adapter tests
AzuriteConfig
__azurite_db_*.json
/__blobstorage__/
/__queuestorage__/
